# Use the H2 in-memory database for tests
spring.datasource.url=jdbc:h2:mem:testdb

# This setting tells Hibernate to create the database schema when the test starts
# and completely drop (delete) it when the test finishes.
spring.jpa.hibernate.ddl-auto=create-drop